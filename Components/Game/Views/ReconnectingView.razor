@* 
    ReconnectingView.razor - Displays reconnection state
*@

<div class="min-h-screen flex items-center justify-center p-4 bg-bg-light dark:bg-bg-dark">
    <div class="text-center space-y-6 max-w-md">
        <div class="w-20 h-20 mx-auto rounded-full bg-primary/20 flex items-center justify-center animate-pulse">
            <span class="material-symbols-outlined text-primary" style="font-size: 48px;">signal_wifi_off</span>
        </div>
        <h1 class="text-2xl font-bold text-slate-900 dark:text-white">Łączenie...</h1>
        <p class="text-slate-600 dark:text-slate-400">Próbuję przywrócić połączenie z serwerem.</p>
        <div class="flex justify-center">
            <div class="w-8 h-8 border-4 border-primary border-t-transparent rounded-full animate-spin"></div>
        </div>
    </div>
</div>

@code {
    
}
