@inherits LayoutComponentBase

<div class="min-h-screen flex flex-col safe-area-top">
    @Body
</div>

<!-- Countdown vignette pulse overlay (controlled via JS interop) -->
<div id="countdown-vignette" class="countdown-vignette"></div>

<!-- Portal target for countdown toast — must be at body root, outside all
     transform/backdrop-filter stacking contexts so position:fixed works -->
<div id="countdown-toast-portal"></div>

<div id="blazor-error-ui">
    Wystąpił nieoczekiwany błąd.
    <a href="" class="reload">Odśwież</a>
    <a class="dismiss">🗙</a>
</div>
