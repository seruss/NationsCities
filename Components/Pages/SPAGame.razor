@page "/"
@page "/room/{RoomCode}"
@rendermode InteractiveServer
@* 
    SPAGame.razor - SPA Entry Point
    Routes:
    - / - Main entry (HomeView)
    - /room/{RoomCode} - Deep link to specific room
*@

<NationsCities.Components.Game.GameFlow RoomCodeFromUrl="@RoomCode" />

@code {
    [Parameter]
    public string? RoomCode { get; set; }
}

